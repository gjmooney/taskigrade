import { DialogContent } from "@/components/ui/dialog";

interface ModalDispplayProps {}

const ModalDispplay = ({}: ModalDispplayProps) => {
  return (
    <DialogContent>
      <div>weeeeeeeee</div>
    </DialogContent>
  );
};
export default ModalDispplay;
